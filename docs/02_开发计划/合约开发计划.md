# MemeHunter 合约开发计划

> **状态**: 🟡 开发中  
> **更新**: 2026-01-27 - Hunt 逻辑已移至后端

---

## 一、设计原则

根据项目规划，合约采用 **最小化链上逻辑** 原则：
- 奖池托管 + Token 发放
- 游戏逻辑完全由后端处理
- 减少 Gas 成本，提升用户体验

---

## 二、合约接口设计

### 已实现

| 指令 | 状态 | 说明 |
|------|------|------|
| `initialize_game` | ✅ 已实现 | 初始化游戏状态 |
| `create_room` | ✅ 已实现 | 创建房间 PDA |
| `authorize_session` | ✅ 已实现 | 授权 Session Key |

### 待实现

| 指令 | 状态 | 说明 |
|------|------|------|
| `deposit_pool` | 📋 待开发 | 项目方充值奖池 (SPL Token) |
| `claim_reward` | 📋 待开发 | 后端签名发放奖励 |
| `settle_room` | 📋 待开发 | 结算房间，退还剩余代币 |

### 已移除

| 指令 | 原因 |
|------|------|
| ~~`hunt`~~ | 逻辑移至后端，减少链上计算 |

---

## 三、核心接口规格

### 3.1 deposit_pool (待实现)

```rust
pub fn deposit_pool(ctx: Context<DepositPool>, amount: u64) -> Result<()>;
```

- **调用方**: 项目方
- **功能**: 向房间奖池充值 SPL Token
- **参数**: `amount` - 充值金额

### 3.2 claim_reward (待实现)

```rust
pub fn claim_reward(
    ctx: Context<ClaimReward>, 
    amount: u64, 
    nonce: u64,
    signature: [u8; 64]  // 后端签名
) -> Result<()>;
```

- **调用方**: 后端 Relayer
- **功能**: 根据后端签名发放奖励
- **验证**: 检查签名有效性，防止重放攻击

### 3.3 settle_room (待实现)

```rust
pub fn settle_room(ctx: Context<SettleRoom>) -> Result<()>;
```

- **调用方**: 项目方
- **功能**: 结算房间，退还剩余代币
- **条件**: 房间已结束或超时

---

## 四、文件结构

```
programs/meme-hunter/src/
├── lib.rs                    # 程序入口
├── instructions/
│   ├── mod.rs
│   ├── initialize.rs         ✅
│   ├── create_room.rs        ✅
│   ├── authorize_session.rs  ✅
│   ├── deposit_pool.rs       📋 待开发
│   ├── claim_reward.rs       📋 待开发
│   └── settle_room.rs        📋 待开发
└── state/
    ├── mod.rs
    ├── room.rs               ✅
    └── session.rs            ✅
```

---

## 五、变更记录

| 日期 | 变更内容 |
|------|----------|
| 2026-01-27 | 移除 `hunt` 指令，逻辑迁移至后端 |
| 2026-01-27 | 规划 `claim_reward`、`deposit_pool`、`settle_room` |
